# 2026-03-09 — Move Spotify Skill to Project-Level

## Summary
Moved the Spotify skill from global (`~/.claude/skills/spotify/SKILL.md`) to project-level (`.claude/skills/spotify/SKILL.md`).

## Changes
- **Created** `.claude/skills/spotify/SKILL.md` with cleaned-up content:
  - Removed hardcoded absolute path `/Users/ishan/lab/spotify-client` (was also incorrect — repo is at `/Users/ishan/lab/tools/spotify-client`)
  - Removed `cd` command (unnecessary for project-level skills, cwd is project root)
  - Removed `sys.path.insert` hack (unnecessary — `uv run` resolves via `pyproject.toml`)
  - Changed `.env` path reference to relative
- **Deleted** `~/.claude/skills/spotify/SKILL.md` (global)

## Why
- Version-controlled and portable
- Auto-loads when working in the project
- No more hardcoded paths to break
